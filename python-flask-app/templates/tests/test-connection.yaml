apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "python-flask-app.fullname" . }}-test-connection"
  labels:
    {{- include "python-flask-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "python-flask-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
