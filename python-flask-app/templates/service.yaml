apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-flask-app.fullname" . }}
  labels:
    {{- include "python-flask-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python-flask-app.selectorLabels" . | nindent 4 }}
